namespace EfbisMuhasebe.Domain.Enums;

/// <summary>
/// Cari hesap türü
/// </summary>
public enum CustomerType
{
    Customer = 1,   // Müşteri (Alıcı)
    Supplier = 2,   // Tedarikçi (Satıcı)
    Both = 3        // Hem Müşteri Hem Tedarikçi
}
